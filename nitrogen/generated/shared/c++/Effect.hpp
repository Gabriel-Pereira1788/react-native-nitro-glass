///
/// Effect.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroglassview {

  /**
   * An enum which can be represented as a JavaScript union (Effect).
   */
  enum class Effect {
    REGULAR      SWIFT_NAME(regular) = 0,
    CLEAR      SWIFT_NAME(clear) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroglassview

namespace margelo::nitro {

  // C++ Effect <> JS Effect (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroglassview::Effect> final {
    static inline margelo::nitro::nitroglassview::Effect fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("regular"): return margelo::nitro::nitroglassview::Effect::REGULAR;
        case hashString("clear"): return margelo::nitro::nitroglassview::Effect::CLEAR;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum Effect - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroglassview::Effect arg) {
      switch (arg) {
        case margelo::nitro::nitroglassview::Effect::REGULAR: return JSIConverter<std::string>::toJSI(runtime, "regular");
        case margelo::nitro::nitroglassview::Effect::CLEAR: return JSIConverter<std::string>::toJSI(runtime, "clear");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert Effect to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("regular"):
        case hashString("clear"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
