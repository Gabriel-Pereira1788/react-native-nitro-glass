///
/// HybridNitroGlassContainerComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridNitroGlassContainerComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitroglassview::views {

  extern const char HybridNitroGlassContainerComponentName[] = "NitroGlassContainer";

  HybridNitroGlassContainerProps::HybridNitroGlassContainerProps(const react::PropsParserContext& context,
                                                                 const HybridNitroGlassContainerProps& sourceProps,
                                                                 const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    spacing([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("spacing", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.spacing;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.spacing);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroGlassContainer.spacing: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroGlassContainerSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroGlassContainerSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroGlassContainer.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridNitroGlassContainerProps::HybridNitroGlassContainerProps(const HybridNitroGlassContainerProps& other):
    react::ViewProps(),
    spacing(other.spacing),
    hybridRef(other.hybridRef) { }

  bool HybridNitroGlassContainerProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("spacing"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroGlassContainerComponentDescriptor::HybridNitroGlassContainerComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridNitroGlassContainerComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                               const std::shared_ptr<const react::Props>& props,
                                                                                               react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroGlassContainerShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroGlassContainerComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridNitroGlassContainerShadowNode&>(shadowNode);
    const HybridNitroGlassContainerProps& props = concreteShadowNode.getConcreteProps();
    HybridNitroGlassContainerState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitroglassview::views
