///
/// HybridNitroGlassViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridNitroGlassViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitroglassview::views {

  extern const char HybridNitroGlassViewComponentName[] = "NitroGlassView";

  HybridNitroGlassViewProps::HybridNitroGlassViewProps(const react::PropsParserContext& context,
                                                       const HybridNitroGlassViewProps& sourceProps,
                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    interactive([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("interactive", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.interactive;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.interactive);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroGlassView.interactive: ") + exc.what());
      }
    }()),
    tintColor([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("tintColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.tintColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.tintColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroGlassView.tintColor: ") + exc.what());
      }
    }()),
    effect([&]() -> CachedProp<Effect> {
      try {
        const react::RawValue* rawValue = rawProps.at("effect", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.effect;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<Effect>::fromRawValue(*runtime, value, sourceProps.effect);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroGlassView.effect: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroGlassViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroGlassViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroGlassView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridNitroGlassViewProps::HybridNitroGlassViewProps(const HybridNitroGlassViewProps& other):
    react::ViewProps(),
    interactive(other.interactive),
    tintColor(other.tintColor),
    effect(other.effect),
    hybridRef(other.hybridRef) { }

  bool HybridNitroGlassViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("interactive"): return true;
      case hashString("tintColor"): return true;
      case hashString("effect"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroGlassViewComponentDescriptor::HybridNitroGlassViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridNitroGlassViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                          const std::shared_ptr<const react::Props>& props,
                                                                                          react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroGlassViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroGlassViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridNitroGlassViewShadowNode&>(shadowNode);
    const HybridNitroGlassViewProps& props = concreteShadowNode.getConcreteProps();
    HybridNitroGlassViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitroglassview::views
